
<h3>
  <a href="<%= user_path(current_user) %>"><img src="http://placehold.it/52x52"></a>
  <%= current_user.first_name %> <%= current_user.last_name  %>
</h3>

  <%= link_to "view my profile", current_user %>

  <%= pluralize(current_user.microposts.count, "micropost") %>
